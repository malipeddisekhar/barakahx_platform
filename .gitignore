# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build output
frontend/dist/
.vite/

# Environment files (never commit secrets)
.env
.env.local
backend/.env
frontend/.env
# Keep .env.production (it only has public API URL, no secrets)

# Uploads – keep the folder, ignore the files inside it
backend/uploads/*
!backend/uploads/.gitkeep

# OS
.DS_Store
Thumbs.db
